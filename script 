-- HADES HUB v8 - FONDO ZAGREUS OFICIAL PNG (Vacía, Solo GUI)
-- Imagen HD de Zagreus (sin fondo) - Aparece al ejecutar

local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/SadOz8/Library/refs/heads/main/Library.txt"))()

-- URL PNG oficial de Zagreus (alta resolución, compatible Roblox)
local ZAGREUS_IMAGE = "https://www.pngkey.com/png/full/2-28240_zagreus-hades-game.png"

-- Ventana con imagen de fondo (roja Hades)
local window_data, window = library:AddWindow("HadesHub", {
    main_color = Color3.fromRGB(220, 20, 60),     -- Rojo Hades 🔥
    min_size = Vector2.new(400, 600),             -- Tamaño ajustado a la imagen vertical
    toggle_key = Enum.KeyCode.Insert,
    can_resize = true,
    background = ZAGREUS_IMAGE                    -- Fondo: Zagreus oficial
})

-- Tabs vacías (sin funciones)
local tab_main = window_data:AddTab("Main")
local tab_scripts = window_data:AddTab("Scripts")
local tab_settings = window_data:AddTab("Settings")

-- Etiqueta simple en Main (opcional, puedes borrar)
tab_main:AddLabel("HadesHub - Listo para scripts")

-- Notificación
game.StarterGui:SetCore("SendNotification", {
    Title = "HadesHub v8";
    Text = "Fondo Zagreus PNG cargado (vacío)";
    Duration = 4;
})

print("HadesHub v8 - GUI vacía con fondo oficial de Zagreus")