-- CARGA DE LIBRERÍA 
local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/EL-INDIGO15/dyros/refs/heads/main/libreria"))() 

-- OBTENER NOMBRE DEL JUGADOR 
local player = game.Players.LocalPlayer 
local displayName = player.DisplayName or player.Name 

-- OPCIONES DE LA VENTANA (separadas para poder cambiar el color después)
local options = {
    main_color = Color3.fromRGB(220, 20, 60),  -- Color inicial (rojo original)
    min_size = Vector2.new(600, 820),
    can_resize = false,
}

-- VENTANA PRINCIPAL
local window = library:AddWindow("David|Bienvenid@ " .. displayName, options)

-- LISTA DE COLORES PARA LA ANIMACIÓN (rojos + naranja + amarillo)
task.spawn(function()
    local hue = 0
    while true do
        hue = hue + 0.005
        if hue > 1 then
            hue = 0
        end

        options.main_color = Color3.fromHSV(hue, 1, 1)

        task.wait(0.03)
    end
end)

local AutoFarm = window:AddTab("FarmOP") 

-- ================= VARIABLES =================
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local player = Players.LocalPlayer

local AutoFarmEnabled = false
local AutoGlitchEnabled = false
local AutoRebirthEnabled = false

local farmThreads = {}
local glitchThreads = {}

local rng = Random.new()
local rng2 = Random.new()

-- ================= FUNCIONES BASE =================
local function stopThreads(tbl)
    for _, t in ipairs(tbl) do
        if t then task.cancel(t) end
    end
    table.clear(tbl)
end

local function getPrompts(folder, positions)
    local prompts = {}
    for _, obj in ipairs(folder:GetDescendants()) do
        if obj:IsA("BasePart") then
            for _, pos in ipairs(positions) do
                if (obj.Position - pos).Magnitude < 0.01 then
                    local p = obj:FindFirstChild("ProximityPromptMachine")
                    if p then
                        table.insert(prompts, p)
                    end
                end
            end
        end
    end
    return prompts
end

-- ================= AUTO FARM =================
local function startAutoFarm()
    local rebirths = player.leaderstats.Rebirths.Value

    local positions = (rebirths < 10) and {
        Vector3.new(-120.77, 5.02, -56.94),
        Vector3.new(-94.25, 5.02, -35.08)
    } or {
        Vector3.new(2747.89, 7.65, 105.53),
        Vector3.new(2747.89, 7.65, 126.65),
        Vector3.new(2743.69, 11.66, 233.43),
        Vector3.new(2743.69, 11.66, 258.49)
    }

    local folder = workspace:WaitForChild("MachinesFolder")
    local remote = ReplicatedStorage.RemotesEvent.MachineActiveEvent

    local prompts = getPrompts(folder, positions)

    table.insert(farmThreads, task.spawn(function()
        while AutoFarmEnabled do
            local p = prompts[rng:NextInteger(1, #prompts)]
            if p and p.Enabled then fireproximityprompt(p) end
            task.wait(0.5)
        end
    end))

    table.insert(farmThreads, task.spawn(function()
        while AutoFarmEnabled do
            remote:FireServer()
            task.wait(0.05)
        end
    end))
end

AutoFarm:AddSwitch("Fast farm", function(state)
    AutoFarmEnabled = state
    stopThreads(farmThreads)

    if state then
        startAutoFarm()
    end
end)

-- ================= AUTO GLITCH =================
local function startAutoGlitch()
    local rebirths = player.leaderstats.Rebirths.Value

    local positions = (rebirths <= 10) and {
        Vector3.new(-41.56, 3.47, 44.41),
        Vector3.new(-24.11, 3.47, 44.41)
    } or {
        Vector3.new(2712.48, 3.71, 299.78),
        Vector3.new(2691.11, 3.71, 299.78),
        Vector3.new(2613.02, 4.78, 289.73),
        Vector3.new(2584.55, 4.78, 289.73)
    }

    local folder = workspace:WaitForChild("MachinesFolder")
    local remote = ReplicatedStorage.RemotesEvent.MachineActiveEvent

    local prompts = getPrompts(folder, positions)

    table.insert(glitchThreads, task.spawn(function()
        while AutoGlitchEnabled do
            local p = prompts[rng2:NextInteger(1, #prompts)]
            if p and p.Enabled then fireproximityprompt(p) end
            task.wait(0.5)
        end
    end))

    table.insert(glitchThreads, task.spawn(function()
        while AutoGlitchEnabled do
            remote:FireServer()
            task.wait(0.05)
        end
    end))
end

AutoFarm:AddSwitch("Fast Glitch", function(state)
    AutoGlitchEnabled = state
    stopThreads(glitchThreads)

    if state then
        startAutoGlitch()
    end
end)

local function getClosestPrompt(targetPos)
    local closest = nil
    local minDist = math.huge

    for _, v in pairs(workspace:GetDescendants()) do
        if v:IsA("ProximityPrompt") then
            local parent = v.Parent
            if parent and parent:IsA("BasePart") then
                local dist = (parent.Position - targetPos).Magnitude
                if dist < minDist then
                    minDist = dist
                    closest = v
                end
            end
        end
    end

    return closest
end

local function startAutoGlitch600()
    local targetPos = Vector3.new(-5959.97, 6.28, -76.56)
    local remote = ReplicatedStorage.RemotesEvent.MachineActiveEvent

    local prompt = getClosestPrompt(targetPos)

    -- 🔥 Hilo 1: pegarte a la máquina
    table.insert(glitchThreads, task.spawn(function()
        while AutoGlitchEnabled do
            local char = player.Character
            if char and char:FindFirstChild("HumanoidRootPart") then
                local hrp = char.HumanoidRootPart

                -- TP exacto
                hrp.CFrame = CFrame.new(targetPos)

                -- activar prompt real
                if prompt and prompt.Enabled then
                    fireproximityprompt(prompt)
                end
            end
            task.wait(0.1)
        end
    end))

    -- ⚡ Hilo 2: farm rápido
    table.insert(glitchThreads, task.spawn(function()
        while AutoGlitchEnabled do
            remote:FireServer()
            task.wait(0)
        end
    end))
end

-- SWITCH
AutoFarm:AddSwitch("Auto Glitch V2", function(state)
    AutoGlitchEnabled = state
    stopThreads(glitchThreads)

    if state then
        startAutoGlitch600()
    end
end)

-- ================= AUTO REBIRTH =================
AutoFarm:AddSwitch("Fast Rebirth", function(state)
    AutoRebirthEnabled = state
end)

task.spawn(function()
    while true do
        if AutoRebirthEnabled then
            ReplicatedStorage.RemotesEvent.RebirthEvent:FireServer()
        end
        task.wait(0.05)
    end
end)

AutoFarm:AddSwitch("Anti Lag", function() 
    for _, v in pairs(game:GetDescendants()) do 
        if v:IsA("ParticleEmitter") or v:IsA("Smoke") or v:IsA("Fire") or v:IsA("Sparkles") then 
            v.Enabled = false 
        end 
    end 

    local lighting = game:GetService("Lighting") 
    lighting.GlobalShadows = false 
    lighting.FogEnd = 9e9 
    lighting.Brightness = 0 

    settings().Rendering.QualityLevel = 1 

    for _, v in pairs(game:GetDescendants()) do 
        if v:IsA("Decal") or v:IsA("Texture") then 
            v.Transparency = 1 
        elseif v:IsA("BasePart") and not v:IsA("MeshPart") then 
            v.Material = Enum.Material.SmoothPlastic 
            if v.Parent and (v.Parent:FindFirstChild("Humanoid") or v.Parent.Parent:FindFirstChild("Humanoid")) then 
            else 
                v.Reflectance = 0 
            end 
        end 
    end 

    for _, v in pairs(lighting:GetChildren()) do 
        if v:IsA("BlurEffect") or v:IsA("SunRaysEffect") or v:IsA("ColorCorrectionEffect") or v:IsA("BloomEffect") or v:IsA("DepthOfFieldEffect") then 
            v.Enabled = false 
        end 
    end 

    game:GetService("StarterGui"):SetCore("SendNotification", { 
        Title = "anti lag activado", 
        Text = "Full optimization applied!", 
        Duration = 5 
    }) 
end) 


local Killer = window:AddTab("KillsOP")


-- ================= AUTO KILL ALL =================
local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer

local AutoKillAllEnabled = false
local killAllThread

local function EquipPunch()
    local backpack = localPlayer:FindFirstChild("Backpack")
    if backpack then
        local tool = backpack:FindFirstChild("Punch")
        if tool and localPlayer.Character and not localPlayer.Character:FindFirstChild("Punch") then
            local humanoid = localPlayer.Character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                humanoid:EquipTool(tool)
            end
        end
    end
end

local function killAllLoop()
    while AutoKillAllEnabled do
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= localPlayer
            and plr.Character
            and plr.Character:FindFirstChild("HumanoidRootPart")
            and localPlayer.Character
            and localPlayer.Character:FindFirstChild("HumanoidRootPart") then

                local myHRP = localPlayer.Character.HumanoidRootPart
                local targetHRP = plr.Character.HumanoidRootPart

                -- Teleport + pegar rápido
                local startTime = tick()
                while tick() - startTime < 0.4 and AutoKillAllEnabled do
                    myHRP.CFrame = targetHRP.CFrame

                    EquipPunch()

                    local punch = localPlayer.Character:FindFirstChild("Punch")
                    if punch then
                        punch:Activate()
                    end

                    task.wait(0.03)
                end
            end
        end
        task.wait(0.05)
    end
end

-- 🔘 SWITCH EN TU GUI
Killer:AddSwitch("Auto Kill a todos", function(state)
    AutoKillAllEnabled = state

    if state then
        killAllThread = task.spawn(killAllLoop)
    else
        if killAllThread then
            task.cancel(killAllThread)
            killAllThread = nil
        end
    end
end)

-- 🔁 REINICIO AUTOMÁTICO AL MORIR
localPlayer.CharacterAdded:Connect(function()
    task.wait(1)
    if AutoKillAllEnabled then
        killAllThread = task.spawn(killAllLoop)
    end
end)


-- 📌 Teleport / Follow System (versión auto-follow desde Dropdown)


local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local following = false
local followTarget = nil

-- 📌 Función: TP detrás del jugador
local function followPlayer(targetPlayer)
    local myChar = LocalPlayer.Character
    local targetChar = targetPlayer.Character

    if not (myChar and targetChar) then return end
    local myHRP = myChar:FindFirstChild("HumanoidRootPart")
    local targetHRP = targetChar:FindFirstChild("HumanoidRootPart")

    if myHRP and targetHRP then
        local followPos = targetHRP.Position - (targetHRP.CFrame.LookVector * 3)
        myHRP.CFrame = CFrame.new(followPos, targetHRP.Position)
    end
end

-- 📌 Dropdown dinámico de jugadores
local followDropdown = Killer:AddDropdown("Auto seguir jugador", function(selectedDisplayName)
    if selectedDisplayName and selectedDisplayName ~= "" then
        -- Buscar jugador por DisplayName
        local target = nil
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr.DisplayName == selectedDisplayName then
                target = plr
                break
            end
        end

        if target then
            followTarget = target.Name -- Guardamos Name real para seguir
            following = true
            print("✅ Started following:", target.Name)

            -- TP inmediato
            followPlayer(target)
        end
    end
end)

-- 📌 Inicializar lista con DisplayNames
for _, player in ipairs(Players:GetPlayers()) do
    if player ~= LocalPlayer then
        followDropdown:Add(player.DisplayName)
    end
end

-- 📌 Actualizar lista cuando entren jugadores
Players.PlayerAdded:Connect(function(player)
    if player ~= LocalPlayer then
        followDropdown:Add(player.DisplayName)
    end
end)

-- 📌 Actualizar lista cuando se vayan jugadores
Players.PlayerRemoving:Connect(function(player)
    -- Limpiamos y agregamos de nuevo
    followDropdown:Clear()
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer then
            followDropdown:Add(plr.DisplayName)
        end
    end

    -- Dejar de seguir si se fue
    if followTarget == player.Name then
        followTarget = nil
        following = false
    end
end)

-- Cuando se va alguien
Players.PlayerRemoving:Connect(function(player)
    if targetDropdownItems[player.Name] then
        targetDropdownItems[player.Name] = nil
        targetDropdown:Clear()
        for _, displayName in pairs(targetDropdownItems) do
            targetDropdown:Add(displayName)
        end
    end

    for i = #targetPlayerNames, 1, -1 do
        if targetPlayerNames[i] == player.Name then
            table.remove(targetPlayerNames, i)
        end
    end
end)


-- 📌 Botón para dejar de seguir
Killer:AddButton("Dejar de Seguir", function()
    following = false
    followTarget = nil
    print("⛔ Stopped following")
end)

-- 📌 Loop de seguimiento automático
task.spawn(function()
    while task.wait(0.01) do
        if following and followTarget then
            local target = Players:FindFirstChild(followTarget)
            if target then
                followPlayer(target)
            else
                following = false
                followTarget = nil
            end
        end
    end
end)
-- 📌 Reintentar cuando respawnees
LocalPlayer.CharacterAdded:Connect(function()
    task.wait(1)
    if following and followTarget then
        local target = Players:FindFirstChild(followTarget)
        if target then
            followPlayer(target)
        end
    end
end)

local autoPunchSafe = false

Killer:AddSwitch("Auto Punch", function(state)
    autoPunchSafe = state

    task.spawn(function()
        while autoPunchSafe do
            local char = LocalPlayer.Character
            if not char then task.wait() continue end

            local humanoid = char:FindFirstChildOfClass("Humanoid")
            local punch = LocalPlayer.Backpack:FindFirstChild("Punch") 
                or char:FindFirstChild("Punch")

            if punch and humanoid then
                -- 🔥 Forzar equipar bien (mejor que parent)
                if punch.Parent ~= char then
                    humanoid:EquipTool(punch)
                    task.wait(0.1)
                end

                -- 🔥 Evitar que se desequipe
                if not char:FindFirstChild("Punch") then
                    humanoid:EquipTool(punch)
                end

                -- 🔥 Golpear (forma más confiable)
                punch:Activate()
            end

            task.wait(1)
        end
    end)
end)

local autoPunchfastSafe = false

Killer:AddSwitch("Auto Punch Fast", function(state)
    autoPunchSafe = state

    task.spawn(function()
        while autoPunchSafe do
            local char = LocalPlayer.Character
            if not char then task.wait() continue end

            local humanoid = char:FindFirstChildOfClass("Humanoid")
            local punch = LocalPlayer.Backpack:FindFirstChild("Punch") 
                or char:FindFirstChild("Punch")

            if punch and humanoid then
                -- 🔥 Forzar equipar bien (mejor que parent)
                if punch.Parent ~= char then
                    humanoid:EquipTool(punch)
                    task.wait(0.1)
                end

                -- 🔥 Evitar que se desequipe
                if not char:FindFirstChild("Punch") then
                    humanoid:EquipTool(punch)
                end

                -- 🔥 Golpear (forma más confiable)
                punch:Activate()
            end

            task.wait(0.0011)
        end
    end)
end)


local Misc = window:AddTab("Misc")

-- ================= MISC =================
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- REMOTES
local OpenPetRemote = ReplicatedStorage:WaitForChild("RemotesEvent"):WaitForChild("OpenPetEvent")

-- ESTADOS
local eggsState = {
    Common = false,
    Rare = false,
    Epic = false,
    Legendary = false,
    Mythic = false,
    Master = false
}

local masterEgg = false
local autoSpin = false

-- ================= AUTO EGGS LOOP =================
task.spawn(function()
    while true do
        if masterEgg then
            for eggName, _ in pairs(eggsState) do
                OpenPetRemote:FireServer(string.lower(eggName))
            end
        else
            for eggName, enabled in pairs(eggsState) do
                if enabled then
                    OpenPetRemote:FireServer(string.lower(eggName))
                end
            end
        end
        task.wait(0.15)
    end
end)

-- ================= SWITCHES EGGS =================

Misc:AddSwitch("Common Egg", function(state)
    eggsState.Common = state
end)

Misc:AddSwitch("Rare Egg", function(state)
    eggsState.Rare = state
end)

Misc:AddSwitch("Epic Egg", function(state)
    eggsState.Epic = state
end)

Misc:AddSwitch("Legendary Egg", function(state)
    eggsState.Legendary = state
end)

Misc:AddSwitch("Mythic Egg", function(state)
    eggsState.Mythic = state
end)

Misc:AddSwitch("Master Eggs", function(state)
    eggsState.Master = state
end)

-- ================= AUTO SPIN =================
Misc:AddSwitch("Auto Spin", function(state)
    autoSpin = state

    if autoSpin then
        task.spawn(function()
            while autoSpin do
                game:GetService("ReplicatedStorage")
                    :WaitForChild("RemotesEvent")
                    :WaitForChild("SpinFunction")
                    :InvokeServer()
                task.wait(0.1)
            end
        end)
    end
end)

-- Auto Collect Rewards
Misc:AddSwitch("Auto Claim Rewards", function(state)
    autoCollect = state

    if autoCollect then
        task.spawn(function()
            while autoCollect do
                for i = 1, 5 do
                    for attempt = 1, 3 do -- 👈 intenta varias veces
                        game:GetService("ReplicatedStorage")
                            :WaitForChild("RemotesEvent")
                            :WaitForChild("rewardClaim")
                            :FireServer("reward"..i)

                        task.wait(0.3)
                    end
                end
                task.wait(2)
            end
        end)
    end
end)

-- AUTO EAT CHOCOLATE (PRO)
Misc:AddSwitch("Auto Eat Chocolate", function(state)
    autoEat = state

    if autoEat then
        task.spawn(function()
            local player = game.Players.LocalPlayer

            while autoEat do
                local character = player.Character
                local backpack = player:FindFirstChild("Backpack")

                if character and backpack then

                    -- BUSCAR CHOCOLATE (BACKPACK + CHARACTER)
                    local chocolate = nil

                    for _, item in pairs(backpack:GetChildren()) do
                        if item:IsA("Tool") and item.Name:lower():find("chocolate") then
                            chocolate = item
                            break
                        end
                    end

                    if not chocolate then
                        for _, item in pairs(character:GetChildren()) do
                            if item:IsA("Tool") and item.Name:lower():find("chocolate") then
                                chocolate = item
                                break
                            end
                        end
                    end

                    -- SI ENCUENTRA CHOCOLATE
                    if chocolate then

                        -- EQUIPAR SI NO ESTÁ EQUIPADO
                        if chocolate.Parent ~= character then
                            chocolate.Parent = character
                            task.wait(0.1) -- más tiempo en mano
                        end

                        -- SPAM MÁS RÁPIDO
                        for i = 1, 5 do
                            if chocolate then
                                chocolate:Activate()
                            end
                            task.wait(0.03) -- súper rápido
                        end
                    end
                end

                task.wait(0.05) -- loop rápido
            end
        end)
    end
end)

-- ================= ANTI AFK PRO =================
local AntiAFK = false
local VirtualUser = game:GetService("VirtualUser")
local AntiAFKConnection = nil

Misc:AddSwitch("Anti AFK", function(state)
    AntiAFK = state

    if AntiAFKConnection then
        AntiAFKConnection:Disconnect()
        AntiAFKConnection = nil
    end

    if state then
        AntiAFKConnection = player.Idled:Connect(function()
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    end
end)


local estadisticas = window:AddTab("Stats Jugadores")

local SelectPlayerName = ""
local SelectedPlayer = nil

-- DROPDOWN
local PlayerDrop = estadisticas:AddDropdown("Select Player", function(Value)
    SelectPlayerName = Value:match("| (.+)")
    SelectedPlayer = game.Players:FindFirstChild(SelectPlayerName)
end)

-- PLAYERS
for _, Plr in pairs(game:GetService("Players"):GetPlayers()) do
    PlayerDrop:Add(Plr.DisplayName .. " | " .. Plr.Name)
end

game.Players.PlayerAdded:Connect(function(plr)
    PlayerDrop:Add(plr.DisplayName .. " | " .. plr.Name)
end)

-- FORMATO
local function FormatNumber(n)
    if typeof(n) ~= "number" then return tostring(n) end
    local suffix = {"","K","M","B","T"}
    local i = 1
    while n >= 1000 and i < #suffix do
        n /= 1000
        i += 1
    end
    return string.format("%.2f", n) .. suffix[i]
end

-- LABELS
local Strong = estadisticas:AddLabel("")
local Endurance = estadisticas:AddLabel("")
local Mobility = estadisticas:AddLabel("")
local Rebirths = estadisticas:AddLabel("")
local Kills = estadisticas:AddLabel("")
local Coins = estadisticas:AddLabel("")
local Emperor = estadisticas:AddLabel("")
local Device = estadisticas:AddLabel("")

-- BUSCAR STAT MEJORADO 🔥
local function findStat(plr, names)
    for _, v in pairs(plr:GetDescendants()) do
        if v:IsA("NumberValue") or v:IsA("StringValue") or v:IsA("IntValue") then
            for _, name in pairs(names) do
                if v.Name:lower():find(name:lower()) then
                    return v.Value
                end
            end
        end
    end
    return "N/A"
end

-- LOOP
task.spawn(function()
    while task.wait(0.3) do
        if SelectedPlayer then

            Strong.Text = "Strong: " .. FormatNumber(findStat(SelectedPlayer, {"strong","strength"}))
            Endurance.Text = "Endurance: " .. FormatNumber(findStat(SelectedPlayer, {"endurance","durability"}))
            Mobility.Text = "Mobility: " .. FormatNumber(findStat(SelectedPlayer, {"mobility","agility"}))
            Rebirths.Text = "Rebirths: " .. FormatNumber(findStat(SelectedPlayer, {"rebirth"}))
            Kills.Text = "Kills: " .. FormatNumber(findStat(SelectedPlayer, {"kill"}))
            Coins.Text = "Coins: " .. FormatNumber(findStat(SelectedPlayer, {"coin","cash"}))

            -- 🔥 ESTAS SON LAS QUE FALTABAN
            Emperor.Text = "Emperor Reps: " .. FormatNumber(findStat(SelectedPlayer, {"Muscle Emperor Reps","muscle emperor reps","MuscleEmperorReps"}))
            Device.Text = "Device: " .. tostring(findStat(SelectedPlayer, {"device"}))

        end
    end
end)

local Dupe = window:AddTab("Dupe")

local HttpService = game:GetService("HttpService")

local loadingLag = false

Dupe:AddSwitch("DupeX10", function(state)
    loadingLag = state

    if state then
        task.spawn(function()
            while loadingLag do
                
                -- 🔥 Pico fuerte de CPU (freeze corto)
                local t = tick()
                while tick() - t < 0.2 do end

                -- 🧠 Micro tirones random (stutter)
                for i = 1, math.random(5,15) do
                    task.wait(math.random() * 0.05)
                end

                -- ⚡ Freeze irregular (como carga)
                if math.random() < 0.4 then
                    local t2 = tick()
                    while tick() - t2 < math.random(0.1, 0.4) do end
                end

                task.wait(0.05)
            end
        end)
    end
end)

local fpsLag = false

Dupe:AddSwitch("Lag", function(state)
    fpsLag = state

    if state then
        task.spawn(function()
            while fpsLag do
                for i = 1, 200 do
                    local part = Instance.new("Part")
                    part.Size = Vector3.new(5,5,5)
                    part.Position = game.Players.LocalPlayer.Character.HumanoidRootPart.Position + Vector3.new(
                        math.random(-20,20),
                        math.random(-20,20),
                        math.random(-20,20)
                    )
                    part.Anchored = false
                    part.Parent = workspace

                    local fire = Instance.new("Fire", part)
                    local smoke = Instance.new("Smoke", part)
                    local spark = Instance.new("Sparkles", part)
                end

                task.wait(0.1)
            end
        end)
    end
end)

Dupe:AddSwitch("Anti Ban")